# Migration `20200626152612-change-period-to-datetime`

This migration has been generated by Ivan Liang at 6/26/2020, 3:26:12 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."AnnualGoal" DROP COLUMN "year",
ADD COLUMN "year" timestamp(3)  NOT NULL ;

ALTER TABLE "public"."MonthlyGoal" DROP COLUMN "month",
ADD COLUMN "month" timestamp(3)  NOT NULL ;

ALTER TABLE "public"."WeeklyGoal" DROP COLUMN "week",
ADD COLUMN "week" timestamp(3)  NOT NULL ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200626125938-add-nicknames-weekly-monthly..20200626152612-change-period-to-datetime
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -28,9 +28,9 @@
   category      String
   nickname      String
   status        String
   createdAt     DateTime      @default(now())
-  year          Int
+  year          DateTime
   user          User          @relation(fields: [userId], references: [id])
   userId        String
   monthlyGoals  MonthlyGoal[]
 }
@@ -42,9 +42,9 @@
   category      String
   nickname      String
   status        String
   createdAt     DateTime     @default(now())
-  month         Int
+  month         DateTime
   user          User         @relation(fields: [userId], references: [id])
   userId        String
   annualGoal    AnnualGoal   @relation(fields: [annualGoalId], references: [id])
   annualGoalId  Int
@@ -57,9 +57,9 @@
   category      String
   nickname      String
   status        String
   createdAt     DateTime    @default(now())
-  week          Int
+  week          DateTime
   user          User        @relation(fields: [userId], references: [id])
   userId        String
   monthlyGoal   MonthlyGoal @relation(fields: [monthlyGoalId], references: [id])
   monthlyGoalId Int
```


